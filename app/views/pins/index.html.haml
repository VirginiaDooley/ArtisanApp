#pins.transitions-enabled
	- @pins.each do |pin|
		.box.panel.panel-default
			background: image-url("image.jpg")
			= link_to (cl_image_tag pin.image.url), pin
			%h2= link_to pin.title, pin
			%p.user
				Submitted by
				= pin.user.email